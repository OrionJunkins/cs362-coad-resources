require 'rails_helper'

RSpec.describe ApplicationHelper, type: :helper do
  describe 'full_title' do
    it 'gives base title if called with no param' do end
    it 'gives a combined title if called with a normal page title' do end
  end
end
